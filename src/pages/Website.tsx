import { useEffect, useMemo, useState } from "react";
import { Link } from "react-router-dom";
import { supabase } from "../lib/supabase";

type Cleaner={id:string;business_name:string|null;slug:string;logo_url:string|null;is_published:boolean|null};
type Site={headline:string|null;description:string|null;hero_url:string|null;primary_color:string;accent_color:string;show_services:boolean;show_about:boolean;show_contact:boolean;is_published:boolean};
type Domain={id:string;domain:string;verification_token:string;verified:boolean;primary_domain:boolean;verified_at:string|null};
const defaults:Site={headline:"",description:"",hero_url:"",primary_color:"#0f172a",accent_color:"#34d399",show_services:true,show_about:true,show_contact:true,is_published:true};
function cleanDomain(value:string){return value.trim().toLowerCase().replace(/^https?:\/\//,"").replace(/\/.*$/,"").replace(/^www\./,"").replace(/\.$/,"")}

export default function Website(){
 const [cleaner,setCleaner]=useState<Cleaner|null>(null);const [site,setSite]=useState<Site>(defaults);const [domains,setDomains]=useState<Domain[]>([]);const [domainInput,setDomainInput]=useState("");const [loading,setLoading]=useState(true);const [saving,setSaving]=useState(false);const [message,setMessage]=useState<string|null>(null);const publicPath=useMemo(()=>cleaner?`/cleaner/${cleaner.slug}`:"",[cleaner]);
 async function loadDomains(cleanerId:string){const {data,error}=await supabase.from("cleaner_domains").select("id,domain,verification_token,verified,primary_domain,verified_at").eq("cleaner_id",cleanerId).order("created_at");if(error)throw error;setDomains((data||[]) as Domain[])}
 useEffect(()=>{(async()=>{try{const {data:{user}}=await supabase.auth.getUser();if(!user)return;const {data:c,error:ce}=await supabase.from("cleaners").select("id,business_name,slug,logo_url,is_published").eq("user_id",user.id).single();if(ce)throw ce;setCleaner(c as Cleaner);const [{data:s,error:se}]=await Promise.all([supabase.from("cleaner_sites").select("headline,description,hero_url,primary_color,accent_color,show_services,show_about,show_contact,is_published").eq("cleaner_id",c.id).maybeSingle(),loadDomains(c.id)]);if(se)throw se;if(s)setSite({...defaults,...(s as Site)})}catch(e:any){setMessage(e?.message||"Could not load website settings.")}finally{setLoading(false)}})()},[]);
 async function save(){if(!cleaner||saving)return;setSaving(true);setMessage(null);try{const payload={cleaner_id:cleaner.id,...site,headline:site.headline?.trim()||null,description:site.description?.trim()||null,hero_url:site.hero_url?.trim()||null,updated_at:new Date().toISOString()};const {error}=await supabase.from("cleaner_sites").upsert(payload,{onConflict:"cleaner_id"});if(error)throw error;setMessage("Website settings saved.")}catch(e:any){setMessage(e?.message||"Could not save website settings.")}finally{setSaving(false)}}
 async function addDomain(){if(!cleaner)return;const domain=cleanDomain(domainInput);if(!domain||!domain.includes(".")){setMessage("Enter a valid domain, for example mycleaningcompany.co.uk.");return}setMessage(null);const {error}=await supabase.from("cleaner_domains").insert({cleaner_id:cleaner.id,domain});if(error){setMessage(error.code==="23505"?"That domain is already connected to an account.":error.message);return}setDomainInput("");await loadDomains(cleaner.id);setMessage("Domain added. Add the DNS records shown below, then verify it.")}
 async function removeDomain(id:string){if(!cleaner)return;const {error}=await supabase.from("cleaner_domains").delete().eq("id",id).eq("cleaner_id",cleaner.id);if(error){setMessage(error.message);return}await loadDomains(cleaner.id)}

 if(loading)return <main className="container mx-auto max-w-6xl px-4 sm:px-6 py-8">Loading website settings…</main>;if(!cleaner)return <main className="container mx-auto max-w-6xl px-4 sm:px-6 py-8">No cleaner profile found.</main>;
 const live=!!cleaner.is_published&&site.is_published;
 return <main className="container mx-auto max-w-6xl px-4 sm:px-6 py-8 space-y-6"><div className="flex items-start justify-between gap-4 flex-wrap"><div><Link to="/dashboard" className="text-sm underline">← Dashboard</Link><h1 className="text-2xl font-bold mt-2">Your Website</h1><p className="muted mt-1">Manage the public website customers see.</p></div><div className="flex gap-2"><a href={publicPath} target="_blank" rel="noreferrer" className="btn">Open website ↗</a><button onClick={save} disabled={saving} className="btn btn-primary">{saving?"Saving…":"Save changes"}</button></div></div>
 {message&&<div className="rounded-xl border bg-white px-4 py-3 text-sm">{message}</div>}
 <section className="card"><div className="card-pad space-y-3"><div className="flex items-center justify-between gap-3"><div><h2 className="font-semibold">Klean.ly website address</h2><p className="muted text-sm">This permanent address always works while your site is published.</p></div><span className={`rounded-full px-3 py-1 text-xs font-semibold ${live?"bg-green-100 text-green-800":"bg-gray-100 text-gray-600"}`}>{live?"Live":"Not public"}</span></div><div className="rounded-xl bg-gray-50 border px-4 py-3 font-mono text-sm break-all">{window.location.origin}{publicPath}</div></div></section>
 <section className="card"><div className="card-pad space-y-4"><div><h2 className="text-lg font-semibold">Custom domain</h2><p className="muted text-sm">Use your own domain so the page looks like your standalone business website.</p></div><div className="flex flex-col sm:flex-row gap-2"><input value={domainInput} onChange={e=>setDomainInput(e.target.value)} placeholder="mycleaningcompany.co.uk" className="flex-1 rounded-xl border px-3 py-2"/><button type="button" onClick={addDomain} className="btn btn-primary">Add domain</button></div>{domains.length===0?<div className="rounded-xl border border-dashed p-4 text-sm text-gray-600">No custom domain connected yet.</div>:<div className="space-y-3">{domains.map(d=><div key={d.id} className="rounded-xl border p-4 space-y-3"><div className="flex items-start justify-between gap-3"><div><div className="font-semibold">{d.domain}</div><div className={`text-xs mt-1 ${d.verified?"text-green-700":"text-amber-700"}`}>{d.verified?"Verified and ready":"Waiting for DNS verification"}</div></div><button onClick={()=>removeDomain(d.id)} className="text-xs underline text-red-700">Remove</button></div>{!d.verified&&<div className="rounded-lg bg-gray-50 p-3 text-sm space-y-2"><div><b>1.</b> Add a CNAME for <code>www</code> pointing to your Netlify site hostname.</div><div><b>2.</b> Add a TXT record named <code>_kleanly</code> with this value:</div><div className="font-mono text-xs break-all rounded bg-white border p-2">{d.verification_token}</div><div className="text-xs text-gray-500">Klean.ly will verify ownership before this hostname can serve the business website. Netlify must also have the domain attached to this site.</div></div>}{d.verified&&<a href={`https://${d.domain}`} target="_blank" rel="noreferrer" className="inline-flex text-sm underline">Open custom domain ↗</a>}</div>)}</div>}</div></section>
 <div className="grid lg:grid-cols-[1fr_0.9fr] gap-6"><section className="card"><div className="card-pad space-y-5"><div><h2 className="text-lg font-semibold">Content & appearance</h2><p className="muted text-sm">Changes appear on your public page after saving.</p></div><label className="block"><span className="text-sm font-medium">Headline</span><input value={site.headline||""} onChange={e=>setSite({...site,headline:e.target.value})} className="mt-1 w-full rounded-xl border px-3 py-2" placeholder={`Professional local cleaning from ${cleaner.business_name||"your business"}`}/></label><label className="block"><span className="text-sm font-medium">Introduction</span><textarea rows={5} value={site.description||""} onChange={e=>setSite({...site,description:e.target.value})} className="mt-1 w-full rounded-xl border px-3 py-2" placeholder="Tell customers what makes your business different."/></label><label className="block"><span className="text-sm font-medium">Hero image URL</span><input value={site.hero_url||""} onChange={e=>setSite({...site,hero_url:e.target.value})} className="mt-1 w-full rounded-xl border px-3 py-2" placeholder="https://…"/></label><div className="grid sm:grid-cols-2 gap-4"><label><span className="text-sm font-medium">Main colour</span><div className="mt-1 flex gap-2"><input type="color" value={site.primary_color} onChange={e=>setSite({...site,primary_color:e.target.value})} className="h-10 w-12"/><input value={site.primary_color} onChange={e=>setSite({...site,primary_color:e.target.value})} className="w-full rounded-xl border px-3 py-2"/></div></label><label><span className="text-sm font-medium">Accent colour</span><div className="mt-1 flex gap-2"><input type="color" value={site.accent_color} onChange={e=>setSite({...site,accent_color:e.target.value})} className="h-10 w-12"/><input value={site.accent_color} onChange={e=>setSite({...site,accent_color:e.target.value})} className="w-full rounded-xl border px-3 py-2"/></div></label></div><div className="space-y-3"><h3 className="font-semibold">Sections</h3>{([['show_services','Show services & prices'],['show_about','Show about section'],['show_contact','Show contact buttons']] as const).map(([key,label])=><label key={key} className="flex items-center justify-between rounded-xl border px-4 py-3"><span className="text-sm font-medium">{label}</span><input type="checkbox" checked={site[key]} onChange={e=>setSite({...site,[key]:e.target.checked})} className="h-5 w-5"/></label>)}</div><label className="flex items-center justify-between rounded-xl border px-4 py-3"><div><div className="text-sm font-semibold">Publish website</div><div className="muted text-xs">Turn this off to hide the website.</div></div><input type="checkbox" checked={site.is_published} onChange={e=>setSite({...site,is_published:e.target.checked})} className="h-5 w-5"/></label></div></section><section className="card overflow-hidden"><div className="card-pad"><h2 className="text-lg font-semibold">Quick preview</h2><p className="muted text-sm mb-4">Preview your colours and headline.</p><div className="overflow-hidden rounded-2xl border bg-white"><div className="p-6 text-white" style={{background:site.primary_color}}>{cleaner.logo_url&&<img src={cleaner.logo_url} alt="Business logo" className="h-14 w-14 object-contain rounded-xl bg-white p-1 mb-5"/>}<div className="text-xs font-bold uppercase tracking-widest mb-2" style={{color:site.accent_color}}>{cleaner.business_name}</div><div className="text-2xl font-black">{site.headline||`Professional local cleaning from ${cleaner.business_name||"us"}`}</div><p className="mt-3 text-sm text-white/80">{site.description||"Your introduction will appear here."}</p><div className="mt-5 inline-flex rounded-xl px-4 py-2 text-sm font-bold" style={{background:site.accent_color,color:site.primary_color}}>Get in touch</div></div></div></div></section></div></main>;
}
